let initialState = {
	friends: [
		{ id: 1, name: 'Alexey' },
		{ id: 2, name: 'Ivan' },
		{ id: 3, name: 'Nataliya' }
	]
}

const sidebarReducer = (state = initialState, action) => {

	return state;
}

export default sidebarReducer;